spring:
  application:
    name: room-reservation
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

server:
  port: 8080
  servlet:
    session:
      cookie:
        secure: true
        http-only: true
