services:
  postgres:
    image: postgres:16
    container_name: room-reservation-postgres
    environment:
      POSTGRES_DB: room_reservation_worker
      POSTGRES_USER: room_reservation
      POSTGRES_PASSWORD: room_reservation
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U room_reservation -d room_reservation_worker"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
